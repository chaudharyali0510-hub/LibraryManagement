@inject IAuthorizationService AuthorizationService
@model IEnumerable<Publisher>

<div class="container">

    <h2>Publisher</h2>

    @if ((await AuthorizationService.AuthorizeAsync(User, "Publishers.Create")).Succeeded)
    {
        <a asp-action="Upsert" class="btn btn-primary">
            Add Publisher
        </a>
    }

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Name</th>
                <th>Address</th>
                <th>Phone</th>
                <th></th>
            </tr>
        </thead>

        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Name
                    </td>

                    <td>
                        @item.Address
                    </td>

                    <td>
                        @item.Phone
                    </td>

                    <td>
                        @if ((await AuthorizationService.AuthorizeAsync(User, "Publishers.Edit")).Succeeded)
                        {
                            <a asp-action="Upsert"
                               asp-route-id="@item.Id"
                               class="btn btn-warning">
                                Edit
                            </a>
                        }

                        @if ((await AuthorizationService.AuthorizeAsync(User, "Publishers.Delete")).Succeeded)
                        {
                            <a asp-action="Delete"
                               asp-route-id="@item.Id"
                               class="btn btn-danger">
                                Delete
                            </a>
                        }
                    </td>
                </tr>
            }

        </tbody>
    </table>

</div>
